//importing packages
import 'package:flutter/material.dart';

class MobilePortraitProfileBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Profile'),
      ],
    );
  }
}
